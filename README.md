## Create virtual environment
```shell
# Windows
python -m venv venv

# Linux / MacOS
python3 -m venv venv
```

## Activate virtual environment
```shell
# Windows
venv\Scripts\activate

# Linux / MacOS
source venv/bin/activate
```